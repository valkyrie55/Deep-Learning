Install anaconda first, then 
1. #install pip using conda
   conda install pip
2. pip3 install --upgrade tensorflow

# create a new environment 
$ conda create -n fashion-mnist-tutorial# activate the environment you just created
$ source activate fashion-mnist-tutorial# install pip 
$ conda install pip# install latest version of Tensorflow
$ pip install --upgrade tensorflow # if you are prompted to upgrade pip
$ pip install --upgrade pip
